% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend-ppm.R
\name{backend_ppm}
\alias{backend_ppm}
\title{SSO keyring backend for Posit Package Manager}
\arguments{
\item{service}{The URL of the repository.}

\item{username}{The username. Must be \verb{__token__} for this backend.}

\item{keyring}{The keyring name. This backend does not support multiple
keyrings, so this argument is ignored.}
}
\value{
A PPM access token string, or \code{NULL} on failure or if the
request is not applicable to this backend.
}
\description{
Initialize the authenticator.

Get the authentication token. This is the main function
that triggers the authentication flow if needed.

This backend does not support setting secrets.

This backend does not support setting secrets.

This backend does not support deleting secrets.

This backend does not support listing secrets.
}
\details{
This backend handles the OAuth 2.0 device flow for user authentication
and token management with Posit Package Manager. It is designed to work
with \code{pak} for installing packages from PPM repositories that require
SSO authentication.

This is a "get-only" backend. It does not support setting, deleting, or
listing secrets directly. The \code{get()} method triggers the authentication
flow if a valid token is not already available.

See \link{backend} for the documentation of the inherited methods.
}
\seealso{
Other keyring backends: 
\code{\link{backend_env}},
\code{\link{backend_file}},
\code{\link{backend_macos}},
\code{\link{backend_secret_service}},
\code{\link{backend_wincred}}
}
\concept{keyring backends}
